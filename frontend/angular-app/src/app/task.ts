export interface Task {
  id: number;
  title: string;
  description: string;
  priority: number;
  confidential: boolean;

  // creation_date: Date;
  // completion_date: Date;
  // last_update: Date;

  // status: string;

  // start_date: Date;
  // due_date: Date;
  // expiration_date: Date;
}
